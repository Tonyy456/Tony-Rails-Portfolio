<h1>New home video</h1>

<%= form_with(model: @home_video) do |form| %>
  <% if @home_video.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@home_video.errors.count, "error") %> prohibited this home_video from being saved:</h2>

      <ul>
        <% @home_video.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :video, style: "display: block; font-size: 1.5em;" %>
    <%= form.file_field :video %>
  </div>
  <br/>

  <div>
    <%= form.submit %>
  </div>
<% end %>

<br>

<div>
  <%= link_to "Back to home videos", home_videos_path %>
</div>
