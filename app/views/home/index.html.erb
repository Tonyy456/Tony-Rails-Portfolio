<% if current_page?(root_url) && HomeVideo.all.length > 0 %>
    <div class="banner-container">       
        <% HomeVideo.all.shuffle.each_with_index do |video, index| %>
            <video muted playsinline id="banner-video-<%= index %>" style='display: none;'>
                <source 
                    src=<%= url_for(video.video) %> 
                    type=<%= video.get_type %> >
            </video>
        <% end %>      
    </div>
<% end %>
<div class="banner-container" style="z-index: 1000; background: rgba(0,0,0,0.6)">   
</div>
<div style="position: absolute; color: white; font-size: 3em; z-index: 10001; width: 100%; left: 0px; ">
    <div style="margin-top: 15vh; display: flex; flex-direction: column; align-content: center;
    width: 100%; position: relative; justify-content: center; flex-wrap: wrap;">
        <span style="font-size: 2em; height: fit-content; display: inline-block; font-family: cursive;"> 
            Anthony D'Alesandro
        </span>
        <span style="font-size:0.5em; color: rgb(200,200,200); height: fit-content; display: inline-block"> 
            Computer Science . Athlete . Game Developement
        </span>
    </div>
</div>
<div style="height: 45vh; margin-bottom: 10px;">

</div>
<h1> Tony D'Alesandro </h1>

